transforms:
  target: dev

  outputs:
    dev:
      type: duckdb
      path: /usr/local/airflow/include/transforms/poke_db.duckdb
      schema: dbt_dev
      threads: 4
    prod:
      type: duckdb
      path: /usr/local/airflow/include/transforms/poke_db.duckdb
      schema: analytics
      threads: 4
